# Meshy API key, used by `pnpm art generate`, `balance` and `actions` (Plan 3b).
# Copy this file to .env and fill it in. .env is gitignored; never commit a real key.
# Create a key in the API section of your Meshy dashboard.
MESHY_API_KEY=
